import ResetpasswordComp from "@/app/_components/auth/reset-password";

const Resetpassword = () => {
  return (
    <main className="auth-page">
      <ResetpasswordComp />
    </main>
  );
};

export default Resetpassword;
